# Ensure pip can prefer wheels (helps avoid Rust builds)
pip>=25.1.1
setuptools>=68.0.0
wheel

# Telegram + webhook support
python-telegram-bot[webhooks]==22.4

# FastAPI + ASGI server
fastapi==0.117.1
uvicorn==0.23.2

# Pydantic pinned to a version with prebuilt wheels for Python 3.13
pydantic==2.11.9
pydantic-core==2.23.4

# Database + HTTP deps
psycopg2-binary==2.9.10
httpx==0.28.1
httpcore==1.0.9
anyio==4.10.0

# Helpers
typing-extensions==4.15.0
certifi==2025.8.3
python-dotenv==1.1.1
